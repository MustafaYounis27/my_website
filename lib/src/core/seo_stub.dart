// Non-web stub: no-op so tests and non-web platforms don't need dart:html

class Seo {
  static void update({
    required String title,
    required String description,
    String? imageUrl,
    String? urlPath,
  }) {}
}
